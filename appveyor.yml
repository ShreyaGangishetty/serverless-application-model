version: 1.0.{build}
image: Ubuntu

environment:
  matrix:
  - TOXENV: py27
  - TOXENV: py36
  - TOXENV: py37
  - TOXENV: py38

build: off

install:
- sh: pip install tox --user
- sh: "PATH=/home/appveyor/.local/bin:$PATH"

test_script:
- tox
